// components/PopupNotification.tsx

import { View } from "react-native";

type PopupNotificationProps = {
  message: string;
  onClose: () => void;
};

export default function PopupNotification({ message, onClose }: PopupNotificationProps) {
  return (
    <View>

    </View>
  );
}
